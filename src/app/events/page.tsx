import { Home } from "@/components/eventPages";

export default function Page() {
  return (
    <main className="h-screen overflow-y-hidden overflow-x-auto bg-black">
        <Home />
    </main>
  );
}
